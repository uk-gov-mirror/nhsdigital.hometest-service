{
  "version": "0.0.1",
  "name": "nhs-home-test-service",
  "description": "NHS Home Test Service",
  "private": true,
  "scripts": {
    "postinstall": "pnpm -C ui install --frozen-lockfile && pnpm -C lambdas install --frozen-lockfile && pnpm -C tests install --frozen-lockfile",
    "test": "pnpm -C ui run test && pnpm -C lambdas run test",
    "test:playwright": "UI_BASE_URL=$(terraform -chdir=local-environment/infra output -raw ui_url) API_BASE_URL=$(terraform -chdir=local-environment/infra output -raw api_base_url) pnpm -C tests run test:chrome",
    "format": "(git diff --name-only --diff-filter=ACMR -z; git diff --name-only --cached --diff-filter=ACMR -z) | sort -zu | xargs -0 sh -c '[ \"$#\" -gt 0 ] && pnpm run format:pre-commit -- \"$@\"' sh",
    "format:pre-commit": "prettier --write --ignore-unknown",
    "lint": "pnpm -C ui run lint && pnpm -C lambdas run lint && pnpm -C tests run lint",
    "build:lambdas": "pnpm -C lambdas run build",
    "package:lambdas": "pnpm -C lambdas run package",
    "start": "pnpm install --frozen-lockfile && pnpm run local:start",
    "stop": "pnpm run local:stop",
    "local:start": "pnpm run local:backend:start && pnpm run local:terraform:init && pnpm run local:deploy && pnpm run local:frontend:up",
    "local:stop": "pnpm run local:terraform:destroy && COMPOSE_PROFILES=* pnpm run local:compose:down",
    "local:restart": "pnpm run local:stop && pnpm run local:start",
    "local:deploy": "NODE_ENV=development pnpm run build:lambdas && pnpm run package:lambdas && pnpm run local:terraform:apply",
    "local:backend:start": "COMPOSE_PROFILES=backend pnpm run local:compose:up && pnpm run local:service:db:migrate",
    "local:frontend:start": "pnpm run local:terraform:env && pnpm run local:frontend:up",
    "local:frontend:restart": "pnpm run local:compose -- up -d --force-recreate ui",
    "local:frontend:up": "COMPOSE_PROFILES=frontend pnpm run local:compose:up",
    "local:service:ui:start": "pnpm run local:compose:up -- ui",
    "local:service:ui:stop": "pnpm run local:compose:down -- ui",
    "local:service:db:start": "pnpm run local:compose:up -- postgres-db",
    "local:service:db:stop": "pnpm run local:compose:down -- postgres-db",
    "local:service:db:migrate": "bash local-environment/scripts/database/db-migrate.sh",
    "local:service:localstack:start": "pnpm run local:compose:up -- localstack",
    "local:service:localstack:stop": "pnpm run local:compose:down -- localstack",
    "local:terraform": "terraform -chdir=local-environment/infra",
    "local:terraform:init": "pnpm run local:terraform init",
    "local:terraform:plan": "pnpm run local:terraform plan",
    "local:terraform:apply": "bash local-environment/scripts/localstack/ensure-localstack-running.sh && pnpm run local:terraform apply -auto-approve -parallelism=30 && pnpm run local:terraform:env",
    "local:terraform:destroy": "pnpm run local:terraform -- destroy -auto-approve -parallelism=30",
    "local:terraform:env": "bash scripts/terraform/post-apply-env-update.sh",
    "local:compose": "docker compose -f local-environment/docker-compose.yml",
    "local:compose:up": "docker compose -f local-environment/docker-compose.yml up -d",
    "local:compose:down": "docker compose -f local-environment/docker-compose.yml down",
    "local:service:lambda:disable": "AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test aws lambda put-function-concurrency --function-name hometest-service-$LAMBDA --reserved-concurrent-executions 0 --endpoint-url http://localhost:4566 --region eu-west-2",
    "local:service:lambda:enable": "AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test aws lambda delete-function-concurrency --function-name hometest-service-$LAMBDA --endpoint-url http://localhost:4566 --region eu-west-2"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "6.0.2",
    "@types/node": "24.12.2",
    "prettier": "3.8.2",
    "typescript": "6.0.2"
  }
}
